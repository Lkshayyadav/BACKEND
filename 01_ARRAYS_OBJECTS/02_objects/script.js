// {} object : An object is a way to store related data and functions together in one place.

var obj = { 
    name : "lakshay",
    age : 18
};
for accessing it  we can use 
obj['name'] or obj.name 

// if we want we can update the age by using same 
obj.name = "kavya"
//  but for making it permanent we use 
Object.freeze(obj);


  